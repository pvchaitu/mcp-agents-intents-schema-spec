# config.yaml

# LLM and API Configuration
llm_config:
  model: "qwen3-coder:480b-cloud" # The LLM model to use
  base_url: "https://ollama.com"     # The base URL for the remote Ollama service
  api_key: "9fde1446fc9e4609a0ab4ac6eee6365f.qtXc1HtQucwVj0GCY0EDzsl2" # Pseudo API key (set this as OPENAI_API_KEY environment variable if needed)
  keep_alive: "5m"
  format: "json"

# MCP Specification and Database Configuration
mcp_config:
  spec_path: "sqlite-mcp-spec_v0.8_revised_staging.json" # Path to the Open-MCP specification file
  db_path: "company_data.db"      # Local path for the mock SQLite database file
  db_alias: "company_db"          # The alias to use for the database connection